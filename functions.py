from clock import get_curr_time

not_present = True


def check_entrance(time_entrance):
    # presença (5) = 
    not_present = False
    # testar se for entre 7:25 e 7:30
        # estando entre esses considera 7:30 (NORMAL)
    
    # se não ==> testar se é antes de 7:45 (ATRASO)

    # senão ==> (FALTA)

    # começa contador

    # se horário > 11:30: not present = True
    # valor presente / 45
    
# 14400 / 2880 == 5

def check_exit(time_exit=None):
    # if time_exit is None and 
    not_present = True
